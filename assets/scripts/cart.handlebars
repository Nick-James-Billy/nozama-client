<div class="item-box">
  {{#each cart.items}}{{> cart-item}}{{/each}}
</div>
<div class="total">Total: ${{cart.total}}</div>
<button type="button" class="btn btn-success btn-s checkout" data-cart-id="{{_id}}">Checkout</button>
